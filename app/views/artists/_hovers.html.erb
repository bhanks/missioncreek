<style>
  <% @artists.each do |artist| %> 
    #artist_<%=artist.id%>:hover{
      background-image: url("<%= artist.image.medium.url %>");
      background-repeat:no-repeat;
      background-size:226px;
      background-position:center;
    }
  <% end %>
</style>
